% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nma_power_sim.R
\name{nma_power_sim}
\alias{nma_power_sim}
\title{Generalized Power Simulation for Arbitrary Node Network Meta-Analysis}
\usage{
nma_power_sim(
  S = 100,
  target_contrast = c("B", "C"),
  network_design,
  pi_base = 0.5,
  tau = 0.2,
  verbose = FALSE,
  n.cores = parallel::detectCores() - 1
)
}
\arguments{
\item{S}{Number of simulation iterations.}

\item{target_contrast}{A character vector of length 2 defining the comparison
of interest (e.g., c("B", "C")). Evaluates the effect of the 2nd vs the 1st.}

\item{network_design}{A data.frame defining all edges in the network. Must contain:
\itemize{
\item \code{t1}: Name of treatment 1 (e.g., "A").
\item \code{t2}: Name of treatment 2 (e.g., "B").
\item \code{k}: Number of studies on this direct comparison (can be 0).
\item \code{OR}: True Odds Ratio of t2 vs t1. If NA, the function will attempt
to infer it assuming statistical consistency from other connected edges.
}}

\item{pi_base}{Baseline event probability for the reference node (the first
treatment found in the network).}

\item{tau}{Between-study heterogeneity (standard deviation).}

\item{verbose}{Logical. If FALSE, suppresses JAGS/MCMC console output.}

\item{n.cores}{Number of cores to use for parallel processing.}
}
\value{
An object of class "nma_power_sim" containing $summary and $iterations.
}
\description{
Evaluates statistical power, ranking accuracy, and bias for a Bayesian NMA
using a unified edge-list dataframe. Supports any network topology.
}
\examples{
\dontrun{
# Define an arbitrary network: A is standard of care.
# B, C, and D are active treatments.
design <- data.frame(
  t1 = c("A", "A", "A", "B"),
  t2 = c("B", "C", "D", "C"),
  k  = c(6,   6,   2,   3),
  OR = c(1.2, 1.4, 1.1, NA) # OR for B vs C is inferred under consistency
)

res <- nma_power_sim(
  S = 100,
  target_contrast = c("B", "C"),
  network_design = design
)
print(res)
}

}
